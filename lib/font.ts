import { Inter } from 'next/font/google'

/** Police partagée par les deux racines de mise en page. */
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-sans',
})
